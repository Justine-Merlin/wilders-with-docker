type Language {
  id: ID!
  label: String!
  scores: [Score]
}

type LanguageWithoutScores {
  id: ID!
  label: String!
}

input LanguageNewWilder {
  id: String!
  label: String!
}

type Query {
  getAllLanguages: [Language]
}